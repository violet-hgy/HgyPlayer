# 可选：项目资源增量（薄）

通用测法已全部在 `portable-skills/image-read`。

项目若仍需「仓库找图 / 缺图策略 / qrc」等增量，写在本项目 `project-bindings.md` 或单独 `asset-overlay.md`，**不要**再维护一份全量 measure。

本 Kit **故意不包含**旧版 `design-measure` 全量文件。
